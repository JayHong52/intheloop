    <div class="container">
        <h1 class="p-3">
            <%= title %> 
        </h1>
        <div class="row">
            <div class="col">
                <table class="table">
                    <thead>
                      <tr>
                        <th scope="col">#</th>
                        <th scope="col">User</th>
                        <th scope="col">Question</th>
                        <th scope="col">Answer</th>
                        <th class="text-end" scope="col">
                            <a href="/survey/add" id="addButton" class="btn btn-dark btn-sm mb-1"><i class="fas fa-plus-circle"></i> Add Survey</a>
                        </th>
                      </tr>
                    </thead>
                    <tbody>
                        <% for (let count = 0; count < survey.length; count++) { %> 
                        <tr>
                            <th scope="row">
                                <%= count + 1 %> 
                            </th>
                            <td><%= survey[count].userName %> </td>
                            <td><%= survey[count].question %> </td>
                            <td><%= survey[count].answer %> </td>
                            <td class="text-end"> 
                                <!-- Modify & Remove Btn -->
                                <a href="/survey/edit/<%= survey[count]._id %>" class="btn btn-secondary btn-sm edit"><i class="fas fa-edit fa-sm"></i> Update </a>
                                <a href="/survey/delete/<%= survey[count]._id %>" class="btn btn-dark btn-sm edit"><i class="fas fa-trash-alt fa-sm"></i>  Delete </a>
                            </td>
                        </tr>
                        <% } %> 
                    </tbody>
                  </table>
            </div>
        </div>
    </div>